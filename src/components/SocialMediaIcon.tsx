import React from 'react'

const SocialMediaIcon = (children) => {
    return (
        <div className="w-[50px] h-[50px] border-[#edff66] border-2 items-center rounded-full">
            {children}
        </div>
    )
}
export default SocialMediaIcon
